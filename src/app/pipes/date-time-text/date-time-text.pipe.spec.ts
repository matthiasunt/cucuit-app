import { DateTimeTextPipe } from './date-time-text.pipe';

describe('DateTimeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
