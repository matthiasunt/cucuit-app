import { TooltipTimeTextPipe } from './tooltip-time-text.pipe';

describe('TooltipTimeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TooltipTimeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
