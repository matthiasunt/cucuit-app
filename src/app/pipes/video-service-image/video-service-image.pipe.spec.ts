import { VideoServiceImagePipe } from './video-service-image.pipe';

describe('VideoServiceImagePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoServiceImagePipe();
    expect(pipe).toBeTruthy();
  });
});
