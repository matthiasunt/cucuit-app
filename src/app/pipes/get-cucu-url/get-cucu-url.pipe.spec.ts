import { CreateCucuUrlPipe } from './get-cucu-url.pipe';

describe('CreateCucuUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateCucuUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
