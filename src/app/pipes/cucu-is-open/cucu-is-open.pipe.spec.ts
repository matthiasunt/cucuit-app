import { CucuIsOpenPipe } from './cucu-is-open.pipe';

describe('CucuIsOpenPipe', () => {
  it('create an instance', () => {
    const pipe = new CucuIsOpenPipe();
    expect(pipe).toBeTruthy();
  });
});
